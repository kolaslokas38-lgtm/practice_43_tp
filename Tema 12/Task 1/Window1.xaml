<Window x:Class="Task.AddGradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить оценку" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добавление оценки" FontSize="16" 
                   FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Студент:" Margin="0,5"/>
        <ComboBox x:Name="cmbStudent" Grid.Row="2" Margin="0,0,0,10"
                  DisplayMemberPath="Name"/>

        <TextBlock Grid.Row="3" Text="Оценка:" Margin="0,5"/>
        <ComboBox x:Name="cmbGrade" Grid.Row="4" Margin="0,0,0,10">
            <ComboBoxItem Content="5"/>
            <ComboBoxItem Content="4"/>
            <ComboBoxItem Content="3"/>
            <ComboBoxItem Content="2"/>
        </ComboBox>

        <TextBlock Grid.Row="5" Text="Комментарий:" Margin="0,5"/>
        <TextBox x:Name="txtComment" Grid.Row="6" Height="60" 
                 TextWrapping="Wrap" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10">
            <Button Content="OK" Width="80" Height="30" 
                    Click="BtnOk_Click" Margin="0,0,10,0"/>
            <Button Content="Отмена" Width="80" Height="30" 
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>   